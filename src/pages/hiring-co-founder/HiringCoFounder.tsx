// HiringCoFounder is defined within Handbook.tsx
// Import Handbook and extract the component
import Handbook from "../Handbook";

// For now, redirect to handbook which contains the HiringCoFounder component
export default function HiringCoFounder(props: { onBackToHandbook?: () => void }) {
  // This is a stub that redirects to the handbook page
  // The full HiringCoFounder component is rendered within Handbook.tsx at /handbook#hiring-co-founder
  return null;
}
